<%= form_for [:admin, @user] do |form| %>

  <%= render 'common/errors', resource: @user %>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :user_name%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
